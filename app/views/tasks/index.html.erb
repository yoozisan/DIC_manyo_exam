<h1>タスク一覧</h1>
<table>
　<tr>
　　<th>タイトル</th>
　　<th>内容</th>
　</tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.detail %></td>
    <td><%= link_to '詳細を確認する', task_path(task.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくタスクを登録する', new_task_path %>
